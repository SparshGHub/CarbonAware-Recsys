services:
  db:
    image: postgres:15
    container_name: terrabite_db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: terrabite
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database:/docker-entrypoint-initdb.d
    networks:
      - terrabite_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build: 
      context: ./api
      dockerfile: Dockerfile
    container_name: terrabite_api
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql+asyncpg://user:password@db/terrabite
    depends_on:
      db:
        condition: service_healthy
    networks:
      - terrabite_network
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - NODE_ENV=production
    container_name: terrabite_frontend
    ports:
      - "3000:3000"
    networks:
      - terrabite_network
    restart: unless-stopped
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
      - NEXT_PUBLIC_USE_MOCK_API=${NEXT_PUBLIC_USE_MOCK_API:-true}
      - NEXT_PUBLIC_MOCK_API_DELAY_MS=${NEXT_PUBLIC_MOCK_API_DELAY_MS:-220}

networks:
  terrabite_network:
    driver: bridge

volumes:
  postgres_data:
